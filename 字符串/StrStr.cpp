#include <iostream>
#include <string>

/// @brief 给定一个 haystack 字符串和一个 needle 字符串，
///        haystack 字符串中找出 needle 字符串出现的第一个位置 (从0开始)。如果不存在，则返回  -1。
///        实际就是实现KMP算法

class Solution
{
public:
    
};